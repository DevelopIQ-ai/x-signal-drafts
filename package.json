{
  "name": "x-signal-drafts",
  "version": "0.1.0",
  "private": true,
  "description": "A draft-only X monitor for high-signal replies and daily post ideas.",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "reply-scan": "node src/cli.mjs reply-scan",
    "daily-draft": "node src/cli.mjs daily-draft",
    "run": "node src/cli.mjs run",
    "test": "node --test",
    "check": "node --check src/cli.mjs && node --check src/lib/*.mjs"
  },
  "license": "MIT"
}
